#!/usr/bin/env bash

set -e

HOOK_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
REPO_ROOT="$(cd "$HOOK_DIR/.." && pwd)"

echo "Przywracanie bazy danych"

"$REPO_ROOT/mysql/restore-db.sh"

echo "Baza danych przywrócona."
